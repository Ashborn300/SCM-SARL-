rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 0. Default open during development / bootstrap
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
